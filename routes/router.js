/*const express = require('express');
const route = express.Router()

const services = require('../services/render');
const controller = require('../controllers/controller');

/**
 *  @description Root Route
 *  @method GET /
 */
/*
route.get('/', services.homeRoutes);

/**
 *  @description add users
 *  @method GET /add-user
 */
/*
route.get('/add_freelncer', services.add_freelancer)

/**
 *  @description for update user
 *  @method GET /update-user
 */
/*
route.get('/update-freelancer', services.update_freelancer)
/*

// API
route.post('/api/freelancers', controller.create);
route.get('/api/freelancers', controller.find);
route.put('/api/freelancers/:id', controller.update);
route.delete('/api/freelancers/:id', controller.delete);


module.exports = route
*/