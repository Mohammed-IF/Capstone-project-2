<div class="container mt-5">
    <h3 class="my-3">Edit the Article...  </h3>
    <form action="/editArticle/<%= article._id %>" method="POST">
        <input type="text" value="<%= article.title %>" name="title" placeholder="Enter Title" class="form-control" required>
        <textarea name="content" cols="30" rows="10" class="mt-3 form-control" placeholder="Enter Content Here..."
            required><%= article.content %></textarea>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <input type="submit" value="Save Changes" class="mt-5 btn btn-md btn-danger">
    </form>
</div>